---
name: Release
on:
  push:
    tags: [v*]
permissions: {}
jobs:
  release:
    uses: suzuki-shunsuke/go-release-workflow/.github/workflows/release.yaml@add-go-license
    with:
      go-version-file: go.mod
      aqua_version: v2.53.3
    permissions:
      contents: write
      id-token: write
      attestations: write
      actions: read
