---
name: Release
on:
  push:
    tags: [v*]
permissions: {}
jobs:
  release:
    uses: suzuki-shunsuke/go-release-workflow/.github/workflows/release.yaml@separate-build-release
    with:
      go-version-file: go.mod
      aqua_version: v2.53.7
    permissions:
      contents: write
      id-token: write
      attestations: write
      actions: read
